 <%= render :partial => '/shared/ztree/ztree_show',:locals => { :act => "index", :hide_tree => @is_hide_dep.hide_tree?, :get_path => ztree_kobe_department_path(@dep), :post_path => move_kobe_departments_path, :current_node_id => @dep.id, :ajax_show_url => (edit_kobe_user_path(params[:u_id]) if params[:u_id].present?) } %>