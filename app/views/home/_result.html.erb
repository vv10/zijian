<div class="cube-portfolio container mt-40 mb-40">
  <div id="grid-container" class="cbp-l-grid-agency">

    <% rs.each do |product| %>
      <%= show_product(product) %>
    <% end %>
  </div>
</div>

<%= paginate rs, :theme => 'channel' %>
